package com.example.myapplication2.model

class Item {
    var icons:Int ? = 0
    var alpha:String ? = null

    constructor(icons: Int?, alpha: String?) {
        this.icons = icons
        this.alpha = alpha
    }
}